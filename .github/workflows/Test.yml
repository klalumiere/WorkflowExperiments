name: Test
on:
  workflow_dispatch:
    inputs:
      firstName:
        description: 'First Name'
        required: true
      lastName:
        description: 'Last Name'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        shell: python
        run: |
          print("${{github.event.inputs.firstName}}")
          print("${{github.event.inputs.lastName}}")
          x = 1 + 1
          print(x)
